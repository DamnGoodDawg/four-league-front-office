{
  "name": "four-league-front-office",
  "private": true,
  "version": "0.1.0",
  "description": "Personal read-only dashboard consolidating my four fantasy football leagues",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.131.1"
  }
}
